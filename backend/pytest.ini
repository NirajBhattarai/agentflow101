[pytest]
testpaths = agents/liquidity/__test__
python_files = test_*.py
python_classes = Test*
python_functions = test_*
pythonpath = .
addopts = 
    -v
    --tb=short
    --strict-markers
markers =
    unit: Unit tests
    integration: Integration tests
    rpc: Tests that require RPC connection

